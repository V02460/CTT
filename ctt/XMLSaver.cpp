#include "XMLSaver.h"

namespace controller {
namespace project {

using ::exception::IOException;
using ::model::saveable::Saveable;

void ProjectSaver::save(QDir path, const Project &project) {
	QFile file(path.absolutePath());
	if (!file.open(QIODevice::WriteOnly)) {
		throw new IOException(QString("File ").append(path.absolutePath()).append(" could not be opened."));
	}
	QTextStream out(&file);
	out << QString("< ? xml version = \"1.0\" ? >") << endl;
	out << endl;
	out << QString("<!-- DO NOT EDIT THIS FILE! -->") << endl;
	out << endl;
	out << QString("<project>") << endl;
	QList<Saveable> elementList;
	// TODO continue here
}

}  // namespace project
}  // namespace controller